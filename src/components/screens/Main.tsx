import React, { ReactElement } from 'react';
import { View } from 'react-native';

interface Props {
  navigation: any;
}

const Main = ({ navigation }: Props): ReactElement => <View />;

export default Main;
